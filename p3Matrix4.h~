#ifndef P3_MATRIX4_H
#define P3_MATRIX4_H

#include "p3Vector4.h"

//matrix four is meant to help four component coordinates in a thre dimensional space
//assits in the transformations and creations of bodies
//matrix4 accepts 16 floating point numbers
//numbers may be in Vector4's..
//it is recommended that the w component is the same for each point

class p3Matrix4{
public:
	//dimaensions are set in seperate methods, to allow for varied submission methods
	p3Matrix4();
	
	//set dimensions from Vector4
	void set(Vector4 co1, Vector4 co2, Vector4 co3, Vector4 co4);

	//TODO: add other input methods

};

#endif
